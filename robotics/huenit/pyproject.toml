[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "joe-controller"
version = "0.1.0"
dependencies = [
    "pynput",
    "pyserial", # Added for direct hardware communication
]
