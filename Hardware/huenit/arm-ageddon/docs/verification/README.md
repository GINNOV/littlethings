# Verification leftovers

`requirements.json` and this directory’s evidence notes describe the **dead**
`camera-ml-app` inspector (task modes, F-gates). They are historical. Do not
run them as the Go-play finish line.

Current product: `docs/go-play.md`. Current plan: `.omo/plans/go-play-huenit.md`.
Ordinary checks: `swift test --parallel`, `scripts/validate-docs.sh`,
`scripts/check-motion-boundary.sh`.
