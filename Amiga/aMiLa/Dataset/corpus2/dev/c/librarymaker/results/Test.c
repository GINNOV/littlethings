/****h* TestTest.c [2.3] ***********************************
*
* NAME
*    TestTest.c
*
* DESCRIPTION
*    This file contains a test driver program for:
*    junk.library, a shared library generated by:
*    LibraryMaker (c) 2004, written by J. T. Steichen.
*
* NOTES
*    junk.library created by J.T. Steichen on Jan-20-2004
***************************************************************
*
*/

#include <stdio.h>

#include <exec/types.h>

#include <AmigaDOSErrs.h>

#include <clib/exec_protos.h>

#include "TestH.h"

struct Library *TestBase = NULL;

/*
** This is a list of the functions to be tested:
** int Open( int parm1, char *parm2 ) (d0/a0);
** int Close( void *parm1 ) (a0);
** int Expunge( void *parm1 ) (a0);
** int Reserved( void ) ();
** int longFunctionName( int parm1, char *parm2, UBYTE *junk ) (d0/a0/a1);
** void foobar( register __d0 ULONG garbage ) (d0);
   
*/

PUBLIC int main( void )
{
   fputs( "Opening...", stderr );

   TestBase = OpenLibrary( "junk.library", 2 );

   printf( "\nTestBase = 0x%08LX\n", TestBase );

   if (TestBase == NULL)   
      {
      fprintf( stderr, "Couldn't open junk.library !\n" );

      return( ERROR_INVALID_RESIDENT_LIBRARY );
      }

   // Here is where you have to place your test code for the library:

   CloseLibrary( TestBase );

   return( RETURN_OK );
}

/* ----------------- END of TestTest.c file! ------------------ */