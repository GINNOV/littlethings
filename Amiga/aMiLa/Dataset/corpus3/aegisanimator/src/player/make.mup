for i in checks clip convert deadpoin function generic geometry jimlib lotsofli lprintf moveclon names opmatrix predrawp raster readscri replay rfont scanedge softtwee symbols tubes
do
	\megamax\mmcc $i.c
	test $? -eq 0 || { echo "compiling $i.c failed"; break; }
done
